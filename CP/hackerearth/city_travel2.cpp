#include<bits/stdc++.h>
using namespace std;
int main()
{
	 long int S,X,N;
   // cin>>S>>X>>N;
    scanf("%ld %ld %ld",&S,&X,&N);
    long int t,y,prefix_sum=0;
    map <long int,long int> travelled; 
    map<long int,long int>:: iterator it;
	for(long int i=0;i<N;i++)
    {
        scanf("%ld %ld",&t,&y);
        travelled.insert(pair <long int,long int> (t,y)); 
    };
    
    
    
    
	long int x=1,previous_sum;
	
	while(true)
    {
    	travelled_prefix
    }
    
    // map <long int,long int> travelled; 
     //map <long int,long int> travelled_actual_prefix;
      
     //map<long int,long int>:: iterator it;
    /*for(long int i=0;i<N;i++)
	{
        scanf("%ld %ld",&t,&y);
        travelled.insert(pair <long int,long int> (t,y)); 
    }*/
    
    

	
	
	
}
